export { log } from "./log";
export { generateUUID } from "./uuid";
import {
  isPossiblePhoneNumber,
  isValidPhoneNumber,
  parsePhoneNumber,
} from "./phoneValidator";

export const utils = {
  isPossiblePhoneNumber,
  isValidPhoneNumber,
  parsePhoneNumber,
};
